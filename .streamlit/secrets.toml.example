# Copia questo file in `secrets.toml` (stessa cartella) e inserisci la tua chiave.
# Il file `secrets.toml` NON viene committato (è in .gitignore).
# Ottieni la chiave da Google AI Studio: https://aistudio.google.com/app/apikey

GEMINI_API_KEY = "LA_TUA_CHIAVE_QUI"
